#ifndef CORE_CLI_H
#define CORE_CLI_H

#include "../core.h"

/* Permet une coversation avec le serveur en mode connecté
 * @param sock: La socket client
 * */
void connectedMode(SOCKET sock);

#endif
